# include <stdio.h>

int main() {
    int A, B, X;

    // Leitura dos valores de A e B
    scanf("%d %d", &A, &B);

    // Cálculo da soma
    X = A + B;

    // Impressão do resultado
    printf("X = %d\n", X);

    return 0;
}